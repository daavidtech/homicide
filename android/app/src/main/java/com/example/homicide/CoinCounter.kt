package com.example.homicide

/*
class CoinCounter: Observer<Collision> {
    private var count = 0;

    constructor(): super() {

    }

    override fun onNotify(event: Collision) {
        when(event.entity) {
            is Coin -> {
                this.count += event.entity.calculateValue();
            }
        }
    }
}*/
